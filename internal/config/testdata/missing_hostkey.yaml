# host_key alanı bilinçli olarak yok — Validate "host_key" içeren hata vermeli.
listen:
   addr: ":2222"

ca:
  key_file: keys/ca_ed25519

database:
   dsn: postgres://postern:postern@localhost:5432/postern?sslmode=disable

recording:
   dir: recordings

