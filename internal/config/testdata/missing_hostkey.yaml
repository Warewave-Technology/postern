# host_key alanı bilinçli olarak yok — Validate "host_key" içeren hata vermeli.
listen:
   addr: ":2222"

ca:
  key_file: keys/ca_ed25519

database:
   path: postern.db

recording:
   dir: recordings

