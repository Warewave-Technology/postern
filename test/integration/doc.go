// Package integration hosts end-to-end tests that run against a real
// OpenSSH server via testcontainers (set up in S1.9).
//
// Test files here carry the `integration` build tag; run them with
// `make test-integration`.
package integration
